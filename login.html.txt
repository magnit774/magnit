<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Magnit Client | Вход в систему</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&display=swap" rel="stylesheet">
    
    <style>
        /* Основа: Глубокий черный фон и паттерн */
        body {
            font-family: 'Orbitron', sans-serif;
            background-color: #050505;
            color: #eeeeee;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-image: radial-gradient(#1a1a1a 1px, transparent 1px);
            background-size: 40px 40px;
        }

        /* Неоновый цвет акцента (Фиолетовый/Голубой) - Должен совпадать с index.html */
        :root {
            --neon-color: #8A2BE2; /* Ярко-фиолетовый */
            --neon-shadow: 0 0 10px #8A2BE2, 0 0 20px #8A2BE2, 0 0 40px rgba(138, 43, 226, 0.7);
        }

        /* Главный контейнер формы */
        .login-box {
            background: #111111;
            padding: 40px;
            border-radius: 5px;
            box-shadow: 0 0 25px rgba(0, 0, 0, 0.9), var(--neon-shadow);
            width: 350px;
            text-align: center;
            border: 2px solid var(--neon-color);
        }

        .login-box h2 {
            font-size: 2em;
            color: var(--neon-color);
            text-shadow: 0 0 5px var(--neon-color);
            margin-bottom: 30px;
            text-transform: uppercase;
        }

        /* Поля ввода (логин, пароль) */
        .input-group {
            margin-bottom: 25px;
            text-align: left;
        }

        .input-group label {
            display: block;
            margin-bottom: 8px;
            color: #cccccc;
            font-size: 0.9em;
            letter-spacing: 1px;
        }

        .input-group input {
            width: 100%;
            padding: 12px 10px;
            border: 1px solid #333333;
            background: #000000;
            color: #00ffcc; /* Неоновый текст при вводе */
            font-family: 'Arial', sans-serif;
            font-size: 1em;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.5);
            transition: border-color 0.3s, box-shadow 0.3s;
            box-sizing: border-box; /* Важно для правильной ширины */
        }
        
        /* Эффект фокуса */
        .input-group input:focus {
            outline: none;
            border-color: var(--neon-color);
            box-shadow: 0 0 8px var(--neon-color);
        }

        /* Кнопка ВХОД */
        .submit-button {
            display: block;
            width: 100%;
            padding: 15px;
            background: var(--neon-color);
            color: #000000;
            text-decoration: none;
            font-size: 1.2em;
            font-weight: bold;
            border: none;
            border-radius: 2px;
            cursor: pointer;
            transition: all 0.4s ease;
            box-shadow: var(--neon-shadow);
            text-transform: uppercase;
            font-family: 'Orbitron', sans-serif;
            margin-top: 30px;
        }

        .submit-button:hover {
            background: #BA55D3;
            box-shadow: 0 0 15px #BA55D3, 0 0 30px #BA55D3;
        }

        /* Блок для ссылки "Регистрация" */
        .register-block {
            margin-top: 35px;
            padding-top: 15px;
            border-top: 1px solid #222;
        }
        
        .register-block p {
            color: #999;
            font-size: 0.8em;
            margin-bottom: 10px;
        }

        .register-link {
            color: #00ffcc; /* Выделение другим неоновым цветом */
            text